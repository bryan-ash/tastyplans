!!!
%html
  %head
    %meta{ :charset => "utf-8" }
    %title OhMabel!
    = stylesheet_link_tag 'style', 'wmd'
    = google_jquery
    = csrf_meta_tag

  %body
    %header
      %nav
        - if user_signed_in?
          = link_to "Sign out", destroy_user_session_path
        - else
          = link_to "Sign in", new_user_session_path
          / 
            or
            = link_to "Sign up", new_user_registration_path
      %section
        - if user_signed_in?
          %section
            = "Signed in as #{current_user.username}."
        %nav
          = link_to 'Home', root_path
          | 
          = link_to 'Contact', 'http://ohmabel.uservoice.com/forums/81175', :class => 'contact_us'

      %hgroup
        %h1= link_to "OhMabel!", root_path
        %h6= link_to "ease the pain of conjuring daily meals", root_path

      .flash
        - flash.each do |name, msg|
          = content_tag :div, msg, :id => "flash_#{name}", :class => "message"

    %menu
      %nav
        %h2 Recipes
        %ul
          %li= link_to 'List all recipes', recipes_path
          %li= link_to 'Find by ingredient', new_recipe_finder_path
          %li= link_to 'Add a new recipe', new_recipe_path
        - unless actions.empty?
          %h2 Actions
          %ul
            - actions.each_pair do |label, path|
              %li= link_to label, path
 
    %article
      = yield

    / 
      infobox would appear at bottom of page if uncommented
      %section#info
        %section#right
          %h2 Newest Users
          %img{ :src => '../images/arrow.gif', :alt => '' } User1
          %br
          %img{ :src => '../images/arrow.gif', :alt => '' } User2
        %section#left
          %h2 Newest Recipes
          %img{ :src => '../images/arrow.gif', :alt => '' } Recipe1
          %br
          %img{ :src => '../images/arrow.gif', :alt => '' } Recipe2
          
    %footer
      Copyright &copy; 2010 Bryan Ash

  = set_focus_to_first_field
  = add_uservoice_form
  = javascript_include_tag "active_form.js", "analytics.js"
