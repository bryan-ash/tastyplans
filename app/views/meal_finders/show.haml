%h1 What ingredients do you have?
= form_for @meal_finder do |form|
  = form.text_field :ingredient, :id => :ingredient
  = form.submit "Try This"

%ul
  - @meal_finder.ingredient_names.each do |ingredient_name|
    %li= ingredient_name

%h2 With those ingredients you could make:

- if @meal_finder.recipes
  %ul
    - @meal_finder.recipes.each do |recipe|
      %li= link_to recipe.name, recipe_path(recipe)
- else
  %h3 Without ingredients, there's not much you can make!

= set_focus_to_id :ingredient